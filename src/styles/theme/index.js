import { createTheme } from "@mui/material/styles";
const theme = createTheme({
  palette: {
    primary: {
      main: "#e2765a",
      dark: "#222222",
      light: "#FFFFFF",
    },
    secondary: {
      main: "#f44336",
      light: "#ccf5fa",
    },
  },
});
export default theme;
